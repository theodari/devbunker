Feature: $ARGUMENTS

DO NOT explain. DO NOT write code in your response. USE TOOLS ONLY.

Step 1 — RED: Write tests (you MUST NOT read src/ files)
1. Call `list` with path "." to see project structure
2. Call `read` on CMakeLists.txt or package.json to find the test framework
3. Call `glob` to find existing test files for patterns
4. Call `write` to create test files in tests/ or test/
5. Call `bash` to run the tests — they must ALL FAIL
6. Say: "RED done. X tests, all failing. Reply 'go' to implement."
7. STOP. Wait for user.

Step 2 — GREEN: Implement (after user says 'go')
1. Call `bash` to run failing tests
2. Call `read` on the failing tests to understand expectations
3. Call `read` on source files to understand the codebase
4. Call `edit` or `write` to implement — minimum code to pass tests
5. Call `bash` to run tests after each change
6. NEVER modify test files
7. Say: "GREEN done. X/Y tests passing."

START NOW with Step 1. Call `list` with path ".".
